{
  "name": "@dakotajp/lambda",
  "version": "0.1.0",
  "private": true,
  "description": "Async worker Lambdas (heavy processing off the request path).",
  "scripts": {
    "typecheck": "tsc --noEmit"
  },
  "dependencies": {
    "@aws-sdk/client-bedrock-runtime": "^3.1094.0",
    "@dakotajp/storage": "0.1.0"
  },
  "devDependencies": {
    "@types/aws-lambda": "^8.10.145",
    "@types/node": "^24",
    "typescript": "^5"
  }
}
